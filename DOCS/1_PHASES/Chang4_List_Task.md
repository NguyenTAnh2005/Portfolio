# Danh sách công việc Chặng 4: Module Management

_Mục tiêu: Xây dựng hệ quản lý các đối tượng (info, timeline, project, achievement )._

## Lưu ý:

- Các công việc đều có cách thực hiện task gần như giống nhau tuy nhiên khi làm việc **chắc chắn vất vả hơn do đòi hỏi việc phân quyền hợp lý**.
- Theo quy trình: Gọi API - Backend giải mã jwt - trả về data nếu oke. Frontend show kết quả qua UI.

### 4.1 Info (Thông tin cá nhân):

- **Backend**:
  - [ ] Hoàn thiện schemas - crud logic - api của model Info về quản lý của Admin.
- **Frontend**:
  - [ ] Thiết lập UI show json thông tin fetch về thông qua API response.
  - [ ] Nâng cấp UI.

### 4.2 Timeline (Tiểu sử bản thân):

- **Backend**:
  - [ ] Hoàn thiện schemas - crud logic - api của model Timeline về quản lý của Admin.
- **Frontend**:
  - [ ] Thiết lập UI show json thông tin fetch về thông qua API response.
  - [ ] Nâng cấp UI.

### 4.3 Project (Dự án đã làm) - Khó khăn nhất trong dự án:

- **Backend**:
  - [ ] Hoàn thiện schemas - crud logic - api của model Project về quản lý của Admin.
- **Frontend**:
  - [ ] Thiết lập UI show json thông tin fetch về thông qua API response.
  - [ ] Nâng cấp UI.

### 4.4 Achievement (Thành tích):

- **Backend**:
  - [ ] Hoàn thiện schemas - crud logic - api của model Achievement về quản lý của Admin.
- **Frontend**:
  - [ ] Thiết lập UI show json thông tin fetch về thông qua API response.
  - [ ] Nâng cấp UI.

### 4.5 System config (Cấu hình hệ thống):

- **Backend**:
  - [ ] Hoàn thiện schemas - crud logic - api của model System Config (đã chạy seed data?, cấu hình khác,..v.v).
- **Frontend**:
  - [ ] Thiết lập UI show json thông tin fetch về thông qua API response.
  - [ ] Nâng cấp UI.
